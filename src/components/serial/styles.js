import styled from "styled-components";

export const SerialContainer = styled.div`
  .AdvancedOptions {
    font-size: 10px;
    cursor: pointer;
  }

  input {
    max-width: 80px;
    text-align: center;
  }
`;
